<div align="center">
  <h1>All Blocmarks</h1>
</div>
<div align="right">
  <% if policy(Topic.new).create? %>
    <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
  <% end %>
</div>

<%= render @topics %>


